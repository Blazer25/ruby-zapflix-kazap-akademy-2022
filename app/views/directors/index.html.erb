<div class="card mb-3 col-12 dark-gray-6">
  <div class="row g-0 rounded dark-gray-6">
    <div class="col">
      <div class="card-body">
        <div class="col">
          <h1 class="d-flex align-itens-center">Diretores</h1>
          <a href="/directors/new" class="d-flex justify-content-end mb-4" style="text-decoration:none;"><button class="botao-rosa">Novo</button></a>
        </div>
        <div class="row">
          <div class="col-12">
            <table class="table table-dark">
              <thead>
                <tr>
                  <th scope="col">Nome</th>
                  <th scope="col">Criado em</th>
                  <th scope="col">Ações</th>
                </tr>
              </thead>
              <tbody>
                <div id="artists">
                  <% @directors.each do |directors| %>
                  </div>
                  <tr>
                    <td>  <%= tratar_valor(directors.nome) %></td>
                    <td>  <%= formatar_data(directors.created_at) %></td>
                    <td><a href="/directors/<%=directors.id %>/edit"><%= image_tag 'editIcon.png'%></a>  <a href="/directors/<%=directors.id %>"><%= image_tag 'deleteIcon.png'%></a>  </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
