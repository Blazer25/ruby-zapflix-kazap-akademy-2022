<%= form_with(model: movie_genre) do |form| %>
  <% if movie_genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie_genre.errors.count, "error") %> prohibited this movie_genre from being saved:</h2>

      <ul>
        <% movie_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="col">
  <div class="form-group">
    <%= form.label :genero, style: "display: block" %>
    <%= form.text_field :genero, class: "w-100" %>
  </div>
</div>

    <div class="mt-4">
      <a href="/movie_genres"><button class="botao-voltar2">Voltar</button></a>
      <input type="submit" value="Enviar" class="botao-excluir">
    </div>
</div>
<% end %>
